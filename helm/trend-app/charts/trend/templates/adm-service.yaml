apiVersion: v1
kind: Service
metadata:
  name: trend-adm
  namespace: trend
spec:
  selector:
    app: trend-adm
  ports:
    - port: {{ .Values.service.adm.port }}
      targetPort: {{ .Values.service.adm.targetPort }}
  type: {{ .Values.service.adm.type }}